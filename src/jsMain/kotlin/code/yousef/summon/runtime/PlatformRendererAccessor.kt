package code.yousef.summon.runtime

// Note: We don't need to redefine getPlatformRenderer since it's already defined in the common module
// and isn't marked with the 'expect' keyword. The JS target will use the implementation from common code.

// This file is intentionally empty to avoid conflicts with the common implementation. 